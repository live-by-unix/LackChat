# LackChat
Encrypted local chat built in Go! Only one file, and very lightweight. 
Basic how to run (requires Go installed)
````bash
# Initialize module if needed
go mod init lackchat
go get golang.org/x/net/websocket
go run main.go
````
